import 'package:flutter/material.dart';

class Undes extends StatelessWidget {
  const Undes({super.key});

  @override
  Widget build(BuildContext context) {
   return Container(
        color: Color(0xFF171818), 
        child: Center(
          
          ),
          
        );
          
  }
}